# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# Port the server listens on locally (Render.com injects PORT automatically)
PORT=3000

# Set to "production" on Render.com; "development" locally shows detailed errors
NODE_ENV=development
